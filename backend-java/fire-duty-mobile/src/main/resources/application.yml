server:
  port: 8087
spring:
  flyway:
    enabled: false
  application:
    name: fire-duty-mobile
  datasource:
    url: jdbc:postgresql://${DB_HOST:192.168.32.156}:${DB_PORT:15432}/${DB_NAME:fire_duty}
    username: ${DB_USER:fire_duty}
    password: ${DB_PASS:gpte1956}
    driver-class-name: org.postgresql.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
  type-aliases-package: com.fireduty.mobile.entity
